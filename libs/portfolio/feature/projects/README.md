# Portfolio Feature Projects

Smart component for the Projects section of the `portfolio` domain — injects `ProjectsStore` and owns the project detail dialog.

Public API import path:
`@portfolio-monorepo/portfolio/feature/projects`
