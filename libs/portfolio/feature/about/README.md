# Portfolio Feature About

Smart component for the About section of the `portfolio` domain — injects `AboutStore` and passes plain data to `portfolio-ui`.

Public API import path:
`@portfolio-monorepo/portfolio/feature/about`
