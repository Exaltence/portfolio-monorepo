# Portfolio UI

Presentational components for the `portfolio` domain — project carousel, card and modal, profile panel, tab group, skill and resume lists, typing title.

Public API import path:
`@portfolio-monorepo/portfolio/ui`
