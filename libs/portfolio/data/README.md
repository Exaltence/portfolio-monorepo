# Portfolio Data

Data access layer for the `portfolio` domain — content models (`about`, `profile`, `project`, `resume-entry`, `skill`, `social-link`) and the NgRx Signal Stores that read them from static JSON via `httpResource()`.

Public API import path:
`@portfolio-monorepo/portfolio/data`
