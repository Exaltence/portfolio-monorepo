# Portfolio Util

Framework-agnostic helpers for the `portfolio` domain: resolves the `{{yearsOfExperience}}` content token from a single career-start constant, so the figure is never written down twice, and wraps carousel indices onto a looping track.

Public API import path:
`@portfolio-monorepo/portfolio/util`
