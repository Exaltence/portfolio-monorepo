# Shared UI

Domain-agnostic presentational components and directives for the `shared` domain — icon, theme toggle, site menu, back-to-top, scroll-spy, no-drag and magic-cursor.

Public API import path:
`@portfolio-monorepo/shared/ui`
