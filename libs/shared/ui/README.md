# Shared UI

Domain-agnostic presentational components and directives for the `shared` domain — icon, theme toggle, site menu, back-to-top, scroll-spy and no-drag.

Public API import path:
`@portfolio-monorepo/shared/ui`
