# Shared Util

Framework-agnostic helpers for the `shared` domain — reduced-motion detection, reading CSS motion-duration tokens so JS timers stay in step with the CSS that owns them, and scroll helpers that fall back to instant scrolling when reduced motion is preferred.

Public API import path:
`@portfolio-monorepo/shared/util`
