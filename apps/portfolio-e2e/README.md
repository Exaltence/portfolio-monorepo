# Portfolio E2E

Playwright end-to-end specs for the `portfolio` app, run against chromium, firefox and webkit. Page objects and helpers live in `libs/test/portfolio-e2e/`.

Run:
`npm exec nx run portfolio-e2e:e2e`
